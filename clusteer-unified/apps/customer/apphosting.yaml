# Firebase App Hosting — Clusteer customer app (clusteer.com)
#
# Secret values live in Secret Manager. For each `secret:` below, run:
#   firebase apphosting:secrets:set <SECRET_NAME>
# (grant access when prompted). NEXT_PUBLIC_* vars are baked into the client
# bundle, so they must be available at BUILD.
#
# Only secrets we hold values for are referenced here — App Hosting fails the
# build on any unresolvable secret. The following are intentionally omitted and
# rely on safe in-code fallbacks until credentials exist:
#   MAX_TRADE_AMOUNT          → defaults to ₦1,000,000 per-trade cap
#   SPRING_BOOT_URL / _API_KEY → resolved via NEXT_PUBLIC_API_URL / NEXT_PUBLIC_SPRING_BOOT_API_KEY
#   KYC_PROVIDER / SMILE_* / YOUVERIFY_API_KEY → KYC provider calls disabled
#   RESEND_API_KEY / EMAIL_FROM → SendGrid is the active email path
#   USE_REDIS_RATE_LIMITING / UPSTASH_* → in-memory rate limiting

runConfig:
  cpu: 1
  memoryMiB: 512
  concurrency: 80
  minInstances: 0
  maxInstances: 4

env:
  # ── App config ──
  - variable: NEXT_PUBLIC_APP_URL
    value: https://clusteer.com
    availability: [BUILD, RUNTIME]

  # ── Firebase (client config — public) ──
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    secret: NEXT_PUBLIC_FIREBASE_API_KEY
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    secret: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    secret: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    secret: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    secret: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_APP_ID
    secret: NEXT_PUBLIC_FIREBASE_APP_ID
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_DATABASE_URL
    secret: NEXT_PUBLIC_FIREBASE_DATABASE_URL
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID
    secret: NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID
    availability: [BUILD, RUNTIME]

  # ── Firebase Admin (server) ──
  - variable: FIREBASE_CLIENT_EMAIL
    secret: FIREBASE_CLIENT_EMAIL
  - variable: FIREBASE_PRIVATE_KEY
    secret: FIREBASE_PRIVATE_KEY

  # ── Backend APIs (Spring Boot + blockchain engine) ──
  # SPRING_BOOT_URL is the absolute server-side base for the deployed
  # Clusteer-Api on Cloud Run (springFetch uses it). NEXT_PUBLIC_API_URL ("/api")
  # stays for the browser → Next API routes.
  - variable: SPRING_BOOT_URL
    secret: SPRING_BOOT_URL
  - variable: NEXT_PUBLIC_API_URL
    secret: NEXT_PUBLIC_API_URL
    availability: [BUILD, RUNTIME]
  # Server-side only — availability RUNTIME (not BUILD) so it is never inlined into the
  # client bundle. The Spring backend doesn't enforce X-API-KEY today; the key is kept
  # pending the gateway-enforcement decision. (Rename off NEXT_PUBLIC_ if dropped/enforced.)
  - variable: NEXT_PUBLIC_SPRING_BOOT_API_KEY
    secret: NEXT_PUBLIC_SPRING_BOOT_API_KEY
    availability: [RUNTIME]
  - variable: BLOCKCHAIN_ENGINE_URL
    secret: BLOCKCHAIN_ENGINE_URL
  - variable: BLOCKCHAIN_ENGINE_API_KEY
    secret: BLOCKCHAIN_ENGINE_API_KEY
  - variable: NEXT_PUBLIC_BLOCKCHAIN_ENGINE_URL
    secret: NEXT_PUBLIC_BLOCKCHAIN_ENGINE_URL
    availability: [BUILD, RUNTIME]
  - variable: NEXT_PUBLIC_BLOCKCHAIN_ENGINE_API_KEY
    secret: NEXT_PUBLIC_BLOCKCHAIN_ENGINE_API_KEY
    availability: [BUILD, RUNTIME]

  # ── Auth ──
  - variable: JWT_SECRET
    secret: JWT_SECRET

  # ── Email / OTP (SendGrid) ──
  - variable: SENDGRID_API_KEY
    secret: SENDGRID_API_KEY
  - variable: SENDGRID_FROM_EMAIL
    secret: SENDGRID_FROM_EMAIL

  # ── Payments ──
  - variable: PAYSTACK_SECRET_KEY
    secret: PAYSTACK_SECRET_KEY
  # ── Analytics (NEXT_PUBLIC_GA_ID / NEXT_PUBLIC_META_PIXEL_ID) — add here,
  #    consent-gated, when GA / Meta Pixel go live. ConsentScripts no-ops
  #    without them. ──
